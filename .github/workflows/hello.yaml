name: hello-world
on:
  push:
    branches: [main]
jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODAY=$(date)" >> $GITHUB_ENV
      - run: echo "Hello, world!"
      - run: echo "Today is $TODAY"
